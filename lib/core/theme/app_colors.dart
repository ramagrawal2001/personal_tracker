import 'package:flutter/material.dart';

class AppColors {
  // Background & Surfaces (Dark SaaS Theme)
  static const Color background = Color(0xFF0F172A); // Slate 900
  static const Color surface = Color(0xFF1E293B);    // Slate 800
  static const Color surfaceLight = Color(0xFF334155); // Slate 700
  static const Color cardBg = Color(0xFF1E293B);
  static const Color cardBgElevated = Color(0xFF26334D);

  // Borders & Dividers
  static const Color border = Color(0xFF334155);
  static const Color borderLight = Color(0xFF475569);

  // Typography
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Brand / Accents
  static const Color primary = Color(0xFF6366F1); // Indigo 500
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color accent = Color(0xFF0EA5E9);  // Sky 500

  // Financial Semantics
  static const Color income = Color(0xFF10B981);    // Emerald 500
  static const Color incomeBg = Color(0xFF064E3B);
  
  static const Color expense = Color(0xFFF43F5E);   // Rose 500
  static const Color expenseBg = Color(0xFF4C0519);

  static const Color transfer = Color(0xFF8B5CF6);  // Violet 500
  static const Color transferBg = Color(0xFF3B0764);

  static const Color creditCard = Color(0xFFF59E0B); // Amber 500
  static const Color loan = Color(0xFFEC4899);       // Pink 500

  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);
  static const Color success = Color(0xFF10B981);
}
